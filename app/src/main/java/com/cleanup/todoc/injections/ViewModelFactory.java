package com.cleanup.todoc.injections;

//TODO create and complete class
/**will create a "T" class for instancied(?) the TaskViewModel(in todolist)*/
public class ViewModelFactory {

    /**add private final DataRepo(2)+executor for asyncTask*/

    /**create constructor(same as TaskViewModel*/

    /**create class T assignable from TaskViewModel*/
}
